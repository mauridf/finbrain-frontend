/**
 * Placeholder — a árvore real de rotas será construída no Bloco 04 (Auth)
 * e Bloco 05 (AppShell). Aqui só confirmamos que a base está de pé.
 */
export default function App() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-background">
      <div className="rounded-card border border-border bg-card p-8 shadow-level-1">
        <h1 className="font-display text-2xl font-semibold text-foreground">
          FinBrain
        </h1>
        <p className="mt-2 text-sm text-brand-neutral">
          Base do frontend pronta. Próximo: design system (Bloco 02).
        </p>
      </div>
    </main>
  )
}